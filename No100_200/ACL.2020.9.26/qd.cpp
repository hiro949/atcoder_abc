#include<bits/stdc++.h>
#define rep(i,n) for ( int i=0; i< (n); ++i )
using namespace std;
using namespace atcoder;
using ll = long long;
using P = pair<int,int>;
// seg tree
// root = [a,b]
// ... @ E n step => [a(n),b(n)]
// => b(n-1)-a(n-1) > K and b(n)-a(n) < K

int main(){
    int n, k;
    cin >> n >> k;
    vector<int> a(n), ;
    rep(i,n){
        cin >> a[i];

   cout << len_max << endl;
    return 0;
}